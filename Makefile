# GNU Make workspace makefile autogenerated by Premake

ifndef config
  config=debug
endif

ifndef verbose
  SILENT = @
endif

ifeq ($(config),debug)
  Spyen_config = debug
  BeerCatcher_config = debug

else ifeq ($(config),release)
  Spyen_config = release
  BeerCatcher_config = release

else ifeq ($(config),dist)
  Spyen_config = dist
  BeerCatcher_config = dist

else
  $(error "invalid configuration $(config)")
endif

PROJECTS := Spyen BeerCatcher

.PHONY: all clean help $(PROJECTS) 

all: $(PROJECTS)

Spyen:
ifneq (,$(Spyen_config))
	@echo "==== Building Spyen ($(Spyen_config)) ===="
	@${MAKE} --no-print-directory -C Spyen -f Makefile config=$(Spyen_config)
endif

BeerCatcher: Spyen
ifneq (,$(BeerCatcher_config))
	@echo "==== Building BeerCatcher ($(BeerCatcher_config)) ===="
	@${MAKE} --no-print-directory -C BeerCatcher -f Makefile config=$(BeerCatcher_config)
endif

clean:
	@${MAKE} --no-print-directory -C Spyen -f Makefile clean
	@${MAKE} --no-print-directory -C BeerCatcher -f Makefile clean

help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  debug"
	@echo "  release"
	@echo "  dist"
	@echo ""
	@echo "TARGETS:"
	@echo "   all (default)"
	@echo "   clean"
	@echo "   Spyen"
	@echo "   BeerCatcher"
	@echo ""
	@echo "For more information, see https://github.com/premake/premake-core/wiki"